#include <gtk/gtk.h>
#include "chat_ui.h"
// #include "msg_server.h"

int main (){
    // int succeed = online();
    // if (succeed == -1){
    //     printf("NO!\n");
    //     return 0;
    // }
    // char name[20];
    // scanf("%s", name);
    // char msg[20];
    // sprintf(msg, "/2*%s;", name);
    // send_msg_to_server (msg, "I have loged in!");
    // get_msg_from_server();
    chat_ui("DaDa");
    return 0;
}